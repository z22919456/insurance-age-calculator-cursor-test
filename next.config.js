/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/insurance-age-calculator-cursor-test",
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
